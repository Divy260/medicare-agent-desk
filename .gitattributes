# Normalise to LF in the repository. Without this, files authored on Windows
# arrive with CRLF and the Linux CI runner treats shell scripts and heredocs
# inconsistently.
* text=auto eol=lf
*.png binary
*.jpg binary
*.pdf binary

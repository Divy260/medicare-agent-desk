# Leave ANTHROPIC_API_KEY unset and the app runs on a deterministic mock model.
# Everything except the model itself is real. Set it to swap in Claude.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Pin an exact model ID in production so a model change is a
# deliberate, reviewed deployment rather than a surprise.
CLAUDE_MODEL=claude-sonnet-5
